meta {
  name: Get by ID
  type: http
  seq: 3
}

get {
  url: {{host}}{{port}}/api/employees/:id
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
}

assert {
  res.status: eq 200
  Number(res.body.id): eq {{id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
